# Coval Benchmarks Dashboard

Next.js 16 (App Router) + Tailwind 4 + TypeScript web application that displays benchmarking results for Coval's speech-to-text and text-to-speech pipelines. The dashboard fetches data from a FastAPI backend (configured via `NEXT_PUBLIC_API_URL`) and renders interactive charts and comparisons for model evaluation.
